---
title: "publication_plots"
author: "Liyenne"
date: "2025-12-04"
output: html_document
knit: (function(input, ...) {
    rmarkdown::render(
      input,
      output_dir = here::here("outputs")
    )
  })
cache: false
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Setup
## packages
```{r}
pkgs <- c(
  # utilities:
  "openxlsx",
  "tidyverse",
  "conflicted",
  # modelling:
  "vegan",
  # plotting:
  "ggh4x",
  "ggrepel",
  "ggpubr"
)

install.packages(pkgs[!pkgs %in% installed.packages()[, "Package"]])
purrr::walk(pkgs, library, character.only = TRUE); rm(pkgs)

conflict_prefer("select", "dplyr")
conflict_prefer("filter", "dplyr")
```

# main
## Fig 2. community composition
```{r}
#| label: data import
community <- read.csv("2_data/2_clean/sf_community.csv") %>% 
  mutate(site_id = str_sub(plot, 1, 2)) %>% 
  select(plot:site, site_id, everything())

total_hits <- community %>% 
  filter(part != "other") %>% 
  group_by(plot, spec) %>% 
  summarise(total_hits = sum(hits)) %>% 
  pivot_wider(names_from = spec, values_from = total_hits) %>% 
  column_to_rownames("plot") %>% 
  select_if(negate(function(col) is.numeric(col) && sum(col) < 1)) #??? what is this

leaf_bm <- community %>%
  filter(part == "leaf") %>% 
  na.omit() %>% 
  select(plot, spec, biomass) %>% 
  pivot_wider(names_from = spec, values_from = biomass) %>% 
  select(!Rholap) %>% 
  column_to_rownames("plot")

meta_data <- readxl::read_excel("2_data/1_raw/community/2025/SF_plot_data_2025.xlsx") %>% 
  mutate(site_id = str_sub(plot, 1, 2)) %>% 
  rename(plot_id = "plot") %>% 
  select(plot_id, site_id, habitat, treatment)
```

```{r NMDS, include = FALSE}
#| label: calculate distance matrices
bm_dist <- leaf_bm %>% 
  vegdist(method = "bray", na.rm = TRUE)

bm_nmds <- bm_dist %>% 
  metaMDS(distance = "bray") %>% 
  scores(tidy = TRUE) %>% 
  filter(score == "sites") %>% 
  select(NMDS1, NMDS2)
  
spec_nmds <- leaf_bm %>%  
  metaMDS(distance = "bray") %>% 
  scores(tidy = TRUE) %>% 
  filter(score == "species") 

meta_nmds <- bm_nmds %>% 
  rownames_to_column("plot_id") %>% 
  right_join(meta_data, bm_nmds, by = "plot_id") %>% 
  select(plot_id, site_id, habitat:treatment, NMDS1, NMDS2) %>% 
  na.omit()
```

```{r NMDS plot (country), echo = FALSE}
#| label: fig 2a. leaf bm NMDS
hull_data <- meta_nmds %>% # find convex hulls
  group_by(habitat, treatment) %>% # for each combination of country and habitat
  arrange(NMDS1, NMDS2) %>%
  slice(chull(NMDS1, NMDS2))

pa <- ggplot() +
  geom_point(
    data = meta_nmds,
    aes(x = NMDS1, y = NMDS2, colour = habitat, shape = treatment),
    size = 3,
    stroke = 1
  ) + # add the point markers
  geom_polygon(
    data = hull_data,
    aes(
      x = NMDS1,
      y = NMDS2,
      color = habitat,
      fill = habitat,
      linetype = treatment
    ),
    alpha = 0.30
  ) +
  scale_colour_manual(values = c("#917D53", "#667E4E", "#A8AA97")) +
  scale_fill_manual(values = c("#917D53", "#667E4E", "#A8AA97")) +
  scale_shape_manual(values = c(16, 8)) +
  geom_text(
    data = spec_nmds,
    aes(x = NMDS1, y = NMDS2, label = label),
    alpha = 0.5
  ) + # add the species labels
  #geom_line(data = meta_nmds, aes(x = NMDS1, y = NMDS2, group = site_id), alpha = 0.3) +
  annotate("text", x = -1, y = 1.2, label = "stress = 0.091") +
  #xlim(min(spec_nmds$NMDS1) * 1.1, max(spec_nmds$NMDS1) * 1.1) +
  theme_classic() +
  theme(
    panel.border = element_rect(color = "black", fill = NA, size = 1),
    strip.background = element_rect(color = "black", size = 1),
    legend.position = "bottom",
    legend.box = "vertical",
    legend.box.just = "left",
    legend.margin = margin(t = -8)
  )
pa
```

```{r}
#| label: fig 2b. leaf bm totals
pb <- community %>% 
  filter(!is.na(group), 
         part == "leaf") %>% 
  mutate(group = factor(group, levels = rev(c("eS", "dS", "G", "F")))) %>% 
  ggplot(aes(x = treatment, y = biomass, fill = group)) +
  geom_col() + 
  facet_wrap(~ habitat) + 
  scale_fill_manual(values = rev(c("#667c13", "#41532d", "#a3a351", "#6a9174")),
                    labels = c("F" = "forbs", "G" = "graminoids", "dS" = "decid. shrubs", "eS" = "evergr. shrubs")) +
  scale_y_continuous(expand = expansion(mult = c(0, .1))) + 
  labs(y = "Leaf biomass (g. dry weight)", x = "Treatment") + 
  theme_classic() +
  theme(legend.position = "bottom",
        legend.margin = margin(t = -8), 
        legend.title = element_blank(),
        axis.text.x = element_text(angle = 25, hjust = 1), 
        panel.border = element_rect(color = "black", fill = NA, size = 1)) + 
  guides(fill = guide_legend(reverse = TRUE, nrow = 2)); pb
```

```{r NMDS plot (country), echo = FALSE}
#| label: export figure
ggarrange(pa, pb, ncol = 2, labels = c("a", "b"), widths = c(4, 3))
ggsave("3_output/1_figures/publication_ready/community_composition.tif", height = 4.5, width = 7)
```

## Fig 3. multivariate traits
```{r}
#| label: import bootstrapped traits
boot_traits_bm <- read.csv("2_data/2_clean/traits/bootstrapped_traits_bmWeighted.csv")

plot_traits <- boot_traits_bm %>% 
  group_by(plot, trait) %>% 
  summarise(boot_mean = mean (mean)) %>% 
  pivot_wider(id_cols = plot, names_from = trait, values_from = boot_mean) %>% 
  column_to_rownames(var = "plot") %>% 
  select(-c(dweight, fweight, leafa)) 

# PCA
plot_pca <- plot_traits %>% 
  scale() %>% 
  rda()

summary(plot_pca)

site_scores <- as.data.frame(plot_pca$CA$u[, 1:2])
site_scores$ID <- as.factor(rownames(site_scores))
trait_scores_a <- as.data.frame(plot_pca$CA$v[, 1:2]) %>% 
  mutate(PC1 = PC1 * -1, 
         PC2 = PC2 * -1)
trait_scores_a$trait <- as.factor(rownames(trait_scores_a)) 

site_scores <- boot_traits_bm %>%
  select(plot:site) %>% 
  unique() %>% 
  rename(ID = plot) %>% 
  right_join(site_scores) %>% 
  mutate(PC1 = PC1 * -1, 
         PC2 = PC2 * -1)
```

```{r}
#| label: fig3a. CWM PCA
trait_labs_a <- c("C:N~ratio", "LDMC", "SLA", "delta^13*C", "delta^15*N", "Plant~height", "'%'~C",  "'%'~N")
names(trait_labs_a) <- c("CN_ratio", "LDMC", "SLA", "d13C", "d15N", "height", "wC",  "wN")

pa <- ggplot() +
  geom_hline(yintercept = 0, linetype = "dotted", alpha = 0.8) + 
  geom_vline(xintercept = 0, linetype = "dotted", alpha = 0.8) +
  geom_segment(data = trait_scores_a, aes(x = 0, y = 0, xend = 0.9 * PC1, yend = 0.9 * PC2), arrow = arrow(type = "closed", length = unit(0.1, "inches")), color = "gray") +
  geom_point(data = site_scores, aes(x = PC1, y = PC2, color = habitat, shape = treatment), size = 3, stroke = 1, alpha = .8) + # add the point markers
  geom_label(data = trait_scores_a, aes(PC1, PC2, label = trait_labs_a), col = "navy", label.size = NA, fill = NA, parse = TRUE) +
  #geom_line(data = site_scores, aes(x = PC1, y = PC2, group = site), alpha = 0.3) + 
  #geom_text(data = data_scores, aes(x = NMDS1, y = NMDS2, label = plot), vjust=0) +  # add the site labels
  scale_shape_manual(values = c(16, 8)) +
  scale_colour_manual(values = c("#917D53", "#667E4E", "#A8AA97")) +
  scale_fill_manual(values = c("#917D53", "#667E4E", "#A8AA97")) +
  xlim(min(trait_scores_a$PC1) * 1.2, max(trait_scores_a$PC1) * 1.1) +
  theme_classic() +
  #guides(color = guide_legend(override.aes = list(shape = NA))) +
  theme(panel.border = element_rect(color = "black", fill = NA, size = 1), 
        strip.background = element_rect(color = "black", size = 1),
        legend.position = "bottom",
        legend.box = "vertical",     
        legend.box.just = "left", 
        legend.margin = margin(t = -8)) + 
  labs(x = "PC1 (75%)", y = "PC2 (14%)", color = "Habitat", shape = "Treatment"); pa

#ggsave("3_output/1_figures/traits/plot_pca.tif", height = 6, width = 6)
```

```{r}
#| label: calculate species PCAs
traits <- read_csv("2_data/2_clean/traits/traits_w.csv")

species_traits <- traits %>% 
  filter(complete.cases(.)) %>% 
  group_by(species) %>% 
  summarise(plant_height = mean(height), 
            LDMC = mean(LDMC), 
            SLA = mean(SLA), 
            N = mean(wN), 
            d15N = mean(d15N), 
            C = mean(wC), 
            d13C = mean(d13C), 
            CN_ratio = mean(CN_ratio)) %>% 
  column_to_rownames(var = "species")

# PCA
species_pca <- species_traits %>% 
  scale() %>% 
  rda()

summary(species_pca)

species_scores <- as.data.frame(species_pca$CA$u[, 1:2])
species_scores$species <- as.factor(rownames(species_scores))
trait_scores_b <- as.data.frame(species_pca$CA$v[, 1:2])
trait_scores_b$trait <- as.factor(rownames(trait_scores_b))

species_scores <- species_scores %>% 
  left_join(spec_list) %>% 
  mutate(group = factor(group, levels = rev(c("eS", "dS", "G", "F")))) 

```

```{r}
#| label: species level PCAs 
trait_labs_b <- c("Plant~height", "LDMC", "SLA",  "'%'~N", "delta^15*N", "'%'~C", "delta^13*C", "C:N~ratio")
names(trait_labs_b) <- c("plant_height", "LDMC", "SLA",  "wN", "d15N", "wC", "d13C", "CN_ratio")

pb <- ggplot() +
  geom_hline(yintercept = 0, linetype = "dotted", alpha = 0.8) + 
  geom_vline(xintercept = 0, linetype = "dotted", alpha = 0.8) +
  geom_segment(data = trait_scores_b, aes(x = 0, y = 0, xend = 0.9 * PC1, yend = 0.9 * PC2), arrow = arrow(type = "closed", length = unit(0.1, "inches")), color = "gray") +
  geom_point(data = species_scores, aes(x = PC1, y = PC2, color = group), size = 3, alpha = 0.70) + # add the point markers
  scale_color_manual(values = rev(c("#667c13", "#41532d", "#a3a351", "#6a9174")),
                    labels = c("F" = "forbs", "G" = "graminoids", "dS" = "decid. shrubs", "eS" = "evergr. shrubs")) +
  geom_text_repel(data = species_scores, aes(x = PC1, y = PC2, label = species, color = group), size = 3.5,  alpha = 0.8) +  # add the species labels
  geom_label(data = trait_scores_b, aes(PC1, PC2, label = trait_labs_b), col = "navy", label.size = NA, fill = NA, parse = TRUE) +
  xlim(min(trait_scores_b$PC1) * 1.2, max(trait_scores_b$PC1) * 1.1) +
  theme_classic() +
  theme(panel.border = element_rect(color = "black", fill = NA, size = 1), 
        strip.background = element_rect(color = "black", size = 1),
        legend.position = "bottom",
        legend.margin = margin(t = -8)) + 
  guides(color = guide_legend(override.aes = list(label = ""), reverse = TRUE, nrow = 2)) +
  labs(x = "PC1 (41%)", y = "PC2 (20%)", color = NULL); pb
  
#ggsave("3_output/1_figures/traits/species_pca.tif", height = 6, width = 6)
```

```{r}
#| label: export figure
ggarrange(pa, pb, ncol = 2, labels = c("a", "b"))

ggsave("3_output/1_figures/publication_ready/multivariate_traits.tif", height = 5, width = 9)

rm(species_scores, site_scores, trait_scores_a, trait_labs_a, trait_scores_b, trait_labs_b) # cleanup
```

## Fig 4. CWMs 
```{r}
#| label: import bootstrapped CWMs
boot_traits_bm <- read.csv("2_data/2_clean/traits/bootstrapped_traits_bmWeighted.csv")

facet_order <- c("height", "SLA", "LDMC", "CN_ratio", "wC", "wN", "d13C", "d15N")
trait_labs <- c("Height", "SLA", "LDMC", "C:N~ratio", "'%'~C", "'%'~N", "delta^13*C", "delta^15*N")
names(trait_labs) <- facet_order

# get model outputs (pairwise)
boot_traits_lm <- read.xlsx("3_output/2_statistics/traits/boot_traits_glmmtmb_final_results.xlsx", sheet = "pair_comparison") %>% 
  filter(trait %in% c("SLA", "LDMC", "height", "wC", "wN", "CN_ratio", "d13C", "d15N")) %>%
  mutate(trait = factor(trait, levels = facet_order),
         significance = if_else(significance == "", "ns", significance),
         treatment = "control") %>% 
  select(trait, habitat, treatment, significance)
  
trait_ranges <- boot_traits_bm %>%
  mutate(trait = factor(trait, levels = facet_order)) %>%
  filter(trait %in% c("SLA", "LDMC", "height", "wC", "wN", "CN_ratio", "d13C", "d15N")) %>% 
  group_by(trait) %>%
  summarise(
    trait_min = min(mean, na.rm = TRUE),
    trait_max = max(mean, na.rm = TRUE),
    trait_range = trait_max - trait_min,
    .groups = "drop"
  )  

sig_positions <- boot_traits_bm %>%
  mutate(trait = factor(trait, levels = facet_order)) %>%
  filter(trait %in% c("SLA", "LDMC", "height", "wC", "wN", "CN_ratio", "d13C", "d15N")) %>% 
  left_join(trait_ranges, by = "trait") %>%
  group_by(trait, habitat) %>%
  summarise(
    q1 = quantile(mean, 0.25, na.rm = TRUE),
    q3 = quantile(mean, 0.75, na.rm = TRUE),
    iqr = q3 - q1,
    lower_whisker = max(q1 - 1.5 * iqr, min(mean, na.rm = TRUE)),
    upper_whisker = min(q3 + 1.5 * iqr, max(mean, na.rm = TRUE)),
    # Use actual max or whisker, whichever is higher
    y_base = max(upper_whisker, na.rm = TRUE),
    y_pos = y_base + (trait_range * 0.13),
    .groups = "drop"
  ) %>% 
  left_join(boot_traits_lm) %>% 
  distinct()
```


```{r}
#| label: fig 4a. bm weighted CWMs
pa <- boot_traits_bm %>% 
  filter(trait %in% c("SLA", "LDMC", "height", "wC", "wN", "CN_ratio", "d13C", "d15N")) %>%
  mutate(trait = factor(trait, levels = facet_order)) %>%
  mutate(habitat = factor(habitat, levels = c("fen", "heath", "ridge"))) %>%
  ggplot(aes(x = habitat, y = mean, fill = treatment)) + 
  geom_boxplot() + 
  geom_text(data = sig_positions, aes(y = y_pos, label = significance), 
                  position = position_dodge(width = 0)) +
  scale_fill_manual(values = c("#999999", "#4da6ff")) +
  scale_y_continuous(expand = expansion(mult = c(0.1, 0.13))
  ) +
  facet_wrap(~ trait, scales = "free", ncol = 4, nrow = 2,
             labeller = as_labeller(trait_labs, label_parsed)) + 
  labs(x = "Habitat", y = "Community weighted mean") + 
  theme_classic() +
  theme(panel.spacing = unit(.0, "lines"),
        panel.border = element_rect(color = "black", fill = NA, size = 1),
        legend.position = "bottom",
        legend.margin = margin(t = -8),
        legend.title = element_blank(),
        strip.background = element_rect(color = "black", size = 1),
        axis.text.x = element_text(angle = 25, hjust = 1))

# add variance partitioning 
varpart_r2 <- read.csv("3_output/2_statistics/traits/varpart_r2.csv") %>% mutate(trait = factor(trait, levels = rev(c("height", "SLA", "LDMC", "CN_ratio", "wC", "wN", "d13C", "d15N"))), var_part = factor(var_part, levels = rev(c("ITV", "covariation", "turnover")))) %>% select(!X)
```

```{r}
#| label: fig 4b. variance partitioning
pb <- varpart_r2 %>% 
  filter(r2 >= 0) %>%
  ggplot(aes(y = trait, x = r2, fill = var_part)) +
  geom_col(position = "fill")  + 
  scale_fill_manual(values = c("#4da6ff", "#999999", "#ff6666")) +
  scale_x_continuous(expand = c(0,0)) + 
  scale_y_discrete(labels = parse(text = rev(trait_labs)), position = "right") + 
  labs(y = "Trait", x = parse(text = "R^2")) + 
  theme_classic() +
  theme(legend.position = "bottom",
        legend.margin = margin(t = -8), 
        legend.title = element_blank(), 
        axis.title.y.right = element_text(angle = 90), 
        panel.border = element_rect(color = "black", fill = NA, size = 1)) + 
  guides(fill = guide_legend(reverse = TRUE)); pb
```
```{r}
#| label: export figure
ggarrange(pa, pb, ncol = 2, labels = c("a", "b"), hjust = c(-1, -16), widths = c(5.5, 2.5))

#ggsave("3_output/1_figures/publication_ready/trait_means.tif", height = 4, width = 8)
```

# ESM
## Fig sx. biomass brms

```{r}
#| label: plot data
plot_data <- readxl::read_excel("2_data/1_raw/community/2025/SF_plot_data_2025.xlsx") %>% 
  select(plot:treatment)

leaf_bm <- community %>% 
  filter(part == "leaf") %>%
  na.omit() %>% 
  group_by(plot, group) %>% 
  summarise(total_bm = sum(biomass)) %>% 
  ungroup() %>% 
  left_join(plot_data) %>% 
  relocate(habitat:treatment, .after = plot)

# brm data
# only hurdle_gamma in case of zeros
gamma0_draws <- readxl::read_excel("3_output/2_statistics/biomass/leafBM_0gamma_brm.xlsx", sheet = "post_draws") %>% 
  filter(habitat == "fen" & group == "eS" | 
           habitat == "heath" & group == "F")  

gamma0_est <- readxl::read_excel("3_output/2_statistics/biomass/leafBM_0gamma_brm.xlsx", sheet = "marginal_estimates") %>% 
  filter(habitat == "fen" & group == "eS" | 
           habitat == "heath" & group == "F")  

gamma_draws <- readxl::read_excel("3_output/2_statistics/biomass/leafBM_gamma_brm.xlsx", sheet = "post_draws") %>% 
  anti_join(gamma0_draws, by = c("treatment", "habitat", "group"))
gamma_est <- readxl::read_excel("3_output/2_statistics/biomass/leafBM_gamma_brm.xlsx", sheet = "marginal_estimates") %>% 
  anti_join(gamma0_est, by = c("treatment", "habitat", "group"))

est <- bind_rows(gamma0_est, gamma_est)
draws <- bind_rows(gamma0_draws, gamma_draws) %>% 
  left_join(est %>% 
              select(treatment:group, .upper)) %>% 
  filter(value <= (.upper * 1.2))
```

```{r}
#| label: biomass brm violin plot
sig_vals <- rep(c("ns", NA), 15)
sig_vals[c(15, 17)] <- "*" # headth dS and eS are siggnificant
est$sig_vals <- sig_vals
rm(sig_vals)
est <- est %>%
  mutate(sig_pos = .upper * 1.3)


leaf_bm_w <- leaf_bm %>%
  pivot_wider(names_from = group, values_from = total_bm) %>%
  mutate(total_bm = rowSums(across(where(is.numeric)))) %>%
  pivot_longer(cols = `F`:total_bm, names_to = "group", values_to = "total_bm")

ggplot() +
  geom_violin(
    data = draws, # posterior distribution
    aes(x = treatment, y = value, fill = treatment), #trim = TRUE,
    color = "white",
    alpha = 0.4
  ) +
  geom_point(
    data = leaf_bm_w,
    aes(x = treatment, y = total_bm, color = treatment)
  ) +
  geom_point(
    data = est, # estimated mean
    aes(x = treatment, y = value),
    alpha = 0.4
  ) +
  geom_errorbar(
    data = est, # 95% CIs
    aes(ymin = .lower, ymax = .upper, x = treatment),
    alpha = 0.4
  ) +
  geom_text(
    data = est,
    aes(x = treatment, y = sig_pos, label = sig_vals),
    position = position_dodge(width = 0)
  ) +
  scale_color_manual(
    values = c("#999999", "#4da6ff")
  ) +
  scale_fill_manual(
    values = c("#999999", "#4da6ff")
  ) +
  facet_grid2(
    rows = vars(habitat),
    cols = vars(group),
    scales = "free_y",
    independent = "y"
  ) +
  theme_classic() +
  theme(
    legend.position = "bottom",
    panel.border = element_rect(color = "black", fill = NA, linewidth = 1)
  )
```